# Build artifact of .claude/skills/apple-reminders (EventKit CLI, macOS only).
# Built on demand by that skill's scripts/build.sh; a compiled binary has no
# business in a diff.
.claude/skills/apple-reminders/scripts/remind-cli

# Python
__pycache__/
*.py[cod]

# JavaScript test/build artifacts
node_modules/
dist/
build/
*.log
.env*

# macOS
.DS_Store

# Local editor artifacts
*.swp
*.tmp
.vscode/
.idea/

# Local, non-shared permission state
.claude/settings.local.json
